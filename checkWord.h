#include "linkedList.h"

int checkDif( char*, char* );
void readText( char*, char***, int* );
void wordToList( char[], LinkedList* );
void makeCorrections( char*, char**, int );
void freeArray( char**, int );
int inputYN( char* );
